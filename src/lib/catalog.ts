// A product the live site sells as a single item has no size options; it gets
// one default variant with this name so it can still go in the cart.
export const DEFAULT_VARIANT_NAME = "יחידה";
export const DEFAULT_VARIANT_SOURCE_VALUE = "default";
