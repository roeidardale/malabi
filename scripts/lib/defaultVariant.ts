// Shared by the scraper and the backfill script (single source of truth lives
// in the app so the storefront can recognise the default variant too).
export { DEFAULT_VARIANT_NAME, DEFAULT_VARIANT_SOURCE_VALUE } from "../../src/lib/catalog";
